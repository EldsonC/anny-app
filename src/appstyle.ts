import styled from "styled-components";

export const AppStyle = styled.div `
    position: fixed;
    top: 50px;
    right: 20px;

    display: flex;
    flex-direction: column;
    gap: 10px;

    z-index: 2500;
`;